# 手机端本地对话程序 — 设计文档

> 个人自用 · 纯前端 PWA · 仅调用 DeepSeek API
> 最后更新：2026-08-01

---

## 1. 项目概述

仿微信手机端的本地对话程序，**纯前端 PWA**，无后端服务。全部数据存储在手机本地（IndexedDB），AI 请求由浏览器直接发往 `api.deepseek.com`。

- **形态**：纯前端 PWA（可添加到手机主屏）
- **服务端**：无（所有逻辑跑在浏览器核心逻辑层）
- **AI 供应商**：仅 DeepSeek（`deepseek-v4-flash` / `deepseek-v4-pro`）
- **旧模型名**：`deepseek-chat` / `deepseek-reasoner` 已于 2026-07-24 弃用，代码内自动映射到 v4-flash
- **用途**：个人自用，单用户

### 数据流

```
手机浏览器（PWA）
├─ 核心逻辑层（src/core/）
│   ├─ 对话树引擎 tree.ts（分支/版本/删除/轮次接续）
│   ├─ 上下文组装 context.ts（X 轮 + 提示词 + 正则）
│   ├─ 费用计算 cost.ts（缓存命中/未命中/峰谷）
│   └─ ID 生成 id.ts（secure context 降级）
├─ API 集成层（src/api/deepseek.ts）
│   ├─ 流式/非流式对话（含思考模式控制、温度、深度）
│   ├─ 模型列表 GET /models
│   └─ 余额 GET /user/balance
├─ 持久化层（src/db/）
│   ├─ Dexie schema（index.ts）
│   ├─ repository.ts：CRUD + applyMessageChanges（事务批量）
│   └─ toPlain()：剥离 Vue 响应式 Proxy，防 DataCloneError
├─ 状态层（src/stores/）
│   ├─ conversation.ts：树操作 + 流式管线 + AI 增强/外观
│   ├─ settings.ts：全局设置 + 模型拉取 + 余额 + 价格迁移
│   └─ ui.ts：Toast / ActionSheet / 全屏编辑器状态
└─ UI 层（src/views/ / src/components/）
    ├─ 微信风格 SettingGroup/SettingRow/HeaderBar
    ├─ 设置主列表 + 5 个二级页面（API/模型/AI/费用/外观）
    ├─ AI 增强整页（朋友资料页风格）
    └─ 消息气泡 + Markdown + 版本指示器 + 候选行
```

---

## 2. 可行性验证（已全部通过 ✅）

| # | 验证项 | 结论 |
|---|--------|------|
| 1 | 浏览器 CORS 直连 DeepSeek | ✅ HTTP 200（verify.html 实测） |
| 2 | 流式 include_usage 返回 usage | ✅ 末尾 chunk 含完整 usage（含缓存字段） |
| 3 | reasoning_content 多轮回传 | ✅ API 接受携带回传；设计选择不回传（省 token） |

---

## 3. 技术栈

| 领域 | 选型 |
|------|------|
| 工程 | Vite + TypeScript 5.x |
| UI | Vue 3 (Composition API) + Tailwind CSS 4 |
| 状态 | Pinia |
| 路由 | vue-router (hash mode) |
| 存储 | IndexedDB + Dexie.js |
| Markdown | markdown-it + highlight.js |
| PWA | vite-plugin-pwa |
| 测试 | vitest + fake-indexeddb |
| 样式 | Tailwind v4 @theme 微信色板 |

---

## 4. 数据模型

### 4.1 MessageNode

```ts
interface MessageNode {
  id: string
  conversationId: string
  role: 'user' | 'assistant'
  parentId: string | null          // null = 根子节点
  content: string                   // assistant = 正则处理后的展示内容
  rawContent?: string               // assistant 原始输出（正则前），追溯用
  reasoning?: string                // 思维链（assistant 专属）
  model?: string                    // 生成该消息的模型
  usage?: {
    promptTokens: number
    completionTokens: number
    totalTokens: number
    cost: number                    // 生成时计算，UI 显示时按当前价格实时重算
    promptCacheHitTokens?: number
    promptCacheMissTokens?: number
  }
  versionIndex: number              // 同父兄弟序号
  createdAt: number
  updatedAt: number
}
```

### 4.2 Conversation

```ts
interface Conversation {
  id: string
  title: string
  model: string                     // 对话级模型
  prompt: string                    // 对话级系统提示词
  regex: string                     // 对话级正则 pattern
  regexReplacement: string          // 替换字符串
  xRounds: number                   // 对话级附带轮数 X
  thinkingEnabled?: boolean         // 对话级思考模式（undefined=跟随全局）
  reasoningEffort?: string          // 对话级思考深度
  temperature?: number              // 对话级温度
  bgImage?: string                  // 对话级背景（覆盖全局）
  avatar?: string                   // 对话级 AI 头像（覆盖全局）
  userAvatar?: string               // 对话级用户头像（覆盖全局）
  pinnedAt?: number                 // 置顶时间
  activePath: string[]              // 当前显示路径（节点 id 序列）
  createdAt: number
  updatedAt: number
}
```

### 4.3 GlobalSettings

```ts
interface GlobalSettings {
  apiKey: string
  defaultModel: string              // 新建对话默认模型（默认 deepseek-v4-flash）
  models: string[]                  // 动态拉取的模型列表（去重）
  costTracking: boolean             // 费用显示开关
  prompt: string                    // 全局默认提示词
  regex: string
  regexReplacement: string
  xRounds: number
  thinkingEnabled: boolean          // 思考模式（默认 true）
  reasoningEffort: string           // 思考深度 low/high/max（默认 high）
  temperature: number | null        // 温度（null=API 默认；思考模式下无效）
  bgImage: string                   // 全局默认背景
  avatar: string                    // 全局默认 AI 头像
  userAvatar: string                // 全局默认用户头像
  peakRule: {
    enabled: boolean                // 分时段计费开关
    multiplier: number              // 高峰乘数（默认 2）
    peaks: { start: string; end: string }[]  // HH:mm 时段
  }
  prices: Record<string, {          // 元/百万 token
    in: number                      // 输入（缓存未命中）
    out: number                     // 输出
    cacheHit: number                // 输入（缓存命中）
  }>
}
```

内置价格（2026-07 官方平峰价，可编辑）：

| 模型 | 输入（未命中） | 输出 | 缓存命中 |
|------|-------------|------|---------|
| deepseek-v4-flash | 1 | 2 | 0.02 |
| deepseek-v4-pro | 3 | 6 | 0.025 |

---

## 5. 核心逻辑层

### 5.1 对话树引擎 `src/core/tree.ts`

**基础函数**：
- `createNode` — 创建节点，强制角色交替不变式
- `childrenOf` — 某父节点下的直接子节点（按 versionIndex 排序）
- `pathToNode` — 从节点向上到根的路径
- `resolveInitialPath` — 沿最新分支走到叶子
- `isValidPath` — 校验路径有效性（首节点必为根子节点）
- `followLatestBranch` — 从节点出发，逐级取最大 versionIndex 走到叶子

**分支操作**：

| 操作 | 函数 | 行为 |
|------|------|------|
| 发送消息 | `sendUserMessage` | 尾部 assistant→追加 user；尾部 user→复用触发回复 |
| AI 回复 | `replyToUser` | 父 user 下新建 assistant 子节点，截断旧路径 |
| 编辑 user | `editUserNode` | 同父新建 user 节点（版本+1），store 层自动触发 AI 回复 |
| 刷新 AI | `refreshAssistant` | 同父新建 assistant 节点（版本+1） |
| 修改 AI | `editAssistantNode` | 就地覆盖 content/reasoning，不产生新节点 |
| 切换版本 | `switchVersion` | 替换同父兄弟 → **auto-extend 到叶子**（followLatestBranch） |
| 沿候选延伸 | `extendPath` | 路径追加候选节点（手动选取备选方案） |
| 删除轮次 | `deleteRound` | 删除 user+所有 assistant 子节点 → 后续节点重挂到 user 的父节点 |
| 子树删除 | `deleteSubtree` | 级联删除整棵子树 |
| 收集子树 | `collectSubtreeIds` | 收集某节点全部后代 id |

**deleteRound 示例**：
```
删除前：U1 → A1 → U2 → A2 → U3 → A3
删除 U2 后：U1 → A1 → U3 → A3  （U3 从 A2 的子节点重挂为 A1 的子节点）
```

**switchVersion auto-extend 示例**：
```
切换前 path = [U1(v2), A2]
点 U1(v1)：replace → [U1(v1)] → followLatest → [U1(v1), A1(v2), U3, A3]
不需要再手动点击候选行
```

### 5.2 上下文组装 `src/core/context.ts`

- `collectPairs`：沿当前路径从尾部向上收集最近 X 对 (user+assistant)，跳过空内容
- `buildMessages`：system prompt + X 对历史 + 当前 user 输入
- `applyRegex`：支持 `/pattern/flags`、捕获组替换；非法 pattern 兜底原样返回

### 5.3 费用计算 `src/core/cost.ts`

- `computeCost(usage, price, peakRule?, when?)`：区分缓存命中/未命中；支持峰谷翻倍
- `isPeakTime(when, rule)`：判断某时刻是否处于高峰
- `formatCost(yuan)`：≥0.01 用 4 位小数，更小用 6 位
- 费用在 UI 显示时**按当前价格实时重算**（改价后旧消息也更新）

### 5.4 模型迁移 `src/utils/model.ts`

- `normalizeModel`：`deepseek-chat`/`deepseek-reasoner` → `deepseek-v4-flash`
- 加载设置时自动迁移 defaultModel + 移除废弃价格条目

### 5.5 ID 生成 `src/utils/id.ts`

- `genId()`：优先 `crypto.randomUUID()`，secure context 不可用（局域网 HTTP）降级为时间戳+随机数

---

## 6. API 集成层

### 6.1 流式对话 `streamChat`

- 请求体含 `stream_options.include_usage` + 思考控制（`thinking.type: disabled` / `reasoning_effort` / `temperature`）
- 逐 chunk 回调 content/reasoning 增量
- 末尾 chunk 解析 usage（含缓存字段）
- 暂停：`AbortController.abort()` → `AbortError` 保留已收部分

### 6.2 其他接口

- `listModels(apiKey)` → 模型列表（去重）
- `getBalance(apiKey)` → 余额信息
- `parseSseLine` / `normalizeUsage` — 可单测的工具函数

---

## 7. 持久化层

### 7.1 表结构

| 表 | 键 | 内容 |
|----|----|------|
| `conversations` | id | Conversation（含 activePath） |
| `messages` | id（conversationId 索引） | MessageNode 全量 |
| `settings` | key | GlobalSettings |

### 7.2 Repository

- `toPlain()` — 剥离 Vue Proxy，防 IndexedDB DataCloneError
- `applyMessageChanges(upserts, deletedIds)` — **事务批量**写回重挂节点 + 删除旧节点
- 所有写入（saveConversation/upsertNodes/saveSettings）经过 `toPlain`

---

## 8. 状态层

### 8.1 conversationStore

关键 action：

| Action | 流程 |
|--------|------|
| `sendUserMessage` | tree → upsert → replyToUserNode → runStream |
| `editUserMessage` | tree → upsert → persist → replyToUserNode（自动触发 AI） |
| `deleteSubtree(user)` | deleteRound → upsertNodes(全部存活) → deleteMessages(removed) → persist |
| `deleteSubtree(assistant)` | collectSubtreeIds → treeDelete → deleteMessages → persist |
| `refreshAIReply` | refreshAssistant → upsert → persist → runStream |
| `editAIReply` | 就地覆盖 → applyRegex → upsert |
| `clearConversation` | 删全部节点 → persist |
| `setEnhance` | 对话级模型/提示词/正则/思考/深度/温度 |
| `setAppearance` | 对话级背景/头像（留空=跟随全局） |
| `setModel` / `rename` / `togglePin` | 单项修改 + 排序 |

流式管线 `runStream`：
- 思考模式/深度/温度 inherit：conversation 级 → settings 全局
- 流式增量写入 `nodes.value`
- 结束后 applyRegex → upsert finalNode（含 usage/cost）
- 错误时保留部分内容 + 设置 error

### 8.2 settingsStore

- `loadSettings`：加载 + 旧默认模型迁移 + 废弃价格清理 + 价格补全 + 模型列表去重
- `fetchModels`：拉取列表 + 自动去重 + 默认模型兜底
- `fetchBalance`：余额查询
- 自动保存：`watch(settings, deep)` 500ms 防抖

### 8.3 uiStore

- `showToast(msg)` — 全局 Toast
- `openActionSheet(items)` / `closeActionSheet` — 底部操作菜单
- `openEditor(opts)` / `closeEditor` — 全屏编辑器（含正则实时测试 + 思维链编辑）

---

## 9. 功能清单（当前状态）

| # | 需求 | 状态 |
|---|------|------|
| 1 | 费用追踪（token + 余额 + 开关 + 峰谷 + 缓存命中） | ✅ 已完成 |
| 2.1 | 复制回复（纯文本/Markdown/思维链） | ✅ 已完成 |
| 2.2 | 修改回复（含思维链，就地覆盖） | ✅ 已完成 |
| 2.3 | Markdown 渲染 + 代码高亮 + 一键复制 | ✅ 已完成 |
| 2.4 | 刷新（生成 AI 版本） | ✅ 已完成 |
| 2.5 | 删除（user→轮次接续 / assistant→子树级联） | ✅ 已完成 |
| 2.6 | 撤回 | ❌ 移除（用户不需要） |
| 2.7 | 暂停（流式中断 + 部分内容保留） | ✅ 已完成 |
| 3 | 输入框自适应 | ✅ 已完成 |
| 4 | 对话分支 + 版本指示器 + auto-extend | ✅ 已完成 |
| 5 | AI增强（提示词/正则/X轮/思考/深度/温度） | ✅ 已完成 |
| 6 | 模型切换（动态拉取 + 默认 + 对话级） | ✅ 已完成 |
| 7 | 仿微信前端（首页 + 设置 + AI增强整页） | ✅ 已完成 |
| 8 | 背景/头像（全局 + 对话级覆盖） | ✅ 已完成 |
| — | 清空聊天 | ✅ 已完成 |
| — | 对话置顶 | ✅ 已完成 |
| — | 对话重命名 | ✅ 已完成 |
| — | 分时段计费（高峰翻倍） | ✅ 已完成 |
| — | 思考模式开关（全局+对话级） | ✅ 已完成 |
| — | 思考深度（low/high/max） | ✅ 已完成 |
| — | 温度（全局+对话级） | ✅ 已完成 |
| — | PWA 图标（icon-192/512） | ✅ 已完成 |

---

## 10. 已确认的设计决策

| 决策 | 说明 |
|------|------|
| 修改 AI 回复 | 就地覆盖（刷新才新建版本） |
| 分支切换 | auto-extend 到叶子（followLatestBranch） |
| 删除 user | 轮次删除 → 后续接续（deleteRound），不等同子树级联 |
| 删除 assistant | 子树级联 |
| 尾部待回复 user | 发送 = 触发该 user 的 AI 回复 |
| 思维链 | 不进上下文（省 token） |
| 流式渲染 | 流式中纯文本追加，结束后完整 Markdown 渲染 |
| 费用公式 | 区分缓存命中/未命中 + 分时段翻倍 |
| 纯前端 PWA | Vue 3 + Tailwind CSS 4 + TypeScript |
