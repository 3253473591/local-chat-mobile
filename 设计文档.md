# 手机端本地对话程序 — 设计文档

> 个人自用 · 纯前端 PWA · 仅调用 DeepSeek API
> 状态：设计已闭环（分支模型语义已确认），前端阶段后续处理

---

## 1. 项目概述

仿微信手机端的本地对话程序，**纯前端 PWA**，无后端服务。数据全部存储在手机本地（IndexedDB），AI 请求由浏览器直接发往 `api.deepseek.com`。

- **形态**：纯前端 PWA（可添加到手机主屏）
- **服务端**：无（所有逻辑跑在浏览器核心逻辑层）
- **AI 供应商**：仅 DeepSeek（`deepseek-chat` / `deepseek-reasoner`）
- **用途**：个人自用，单用户

### 数据流

```
手机浏览器（PWA）
├─ 核心逻辑层（相当于"后端"职责）
│   ├─ 对话树引擎（分支/版本）
│   ├─ 上下文组装（X 轮 + 提示词 + 正则后内容）
│   ├─ API 集成层（直连 api.deepseek.com）
│   └─ 费用追踪 / 正则管线
├─ IndexedDB（对话树 + 对话设置 + 全局设置）
└─ UI 层（仿微信，前端阶段实现）
```

---

## 2. 架构形态与可行性门禁

### 2.1 可行性门禁（开工第一步）

纯前端直连 DeepSeek 依赖浏览器跨域（CORS），**必须先实测**：

| # | 验证项 | 结论（2026-07-31 实测） |
|---|--------|----------|
| 1 | `api.deepseek.com` 允许浏览器跨域请求 | ✅ **通过**：浏览器直连 HTTP 200（verify.html 测试1），纯前端方案成立 |
| 2 | 流式模式下 `stream_options.include_usage` 返回 usage | ✅ **通过**：流式末尾 chunk 收到完整 usage，含 `prompt_tokens`/`completion_tokens` 及缓存字段 |
| 3 | 多轮历史中 assistant 消息可/需回传 `reasoning_content` | ✅ **通过**：携带 `reasoning_content` 回传亦被接受（3b 成功）；设计仍选择**不回传**（省 token） |

### 2.2 API Key 安全说明

纯前端下 API Key 存在手机本地（IndexedDB/localStorage）。个人自用场景可接受；风险为拿到手机即可读取 Key。

### 2.3 回退方案

若 CORS 被拦截，退化为"本地极简代理"：一个 ~50 行的本地 Node 静态代理（`express` + `http-proxy`），仅做 CORS 透传，业务逻辑仍在浏览器。文档其余部分不受影响。

---

## 3. 技术栈

| 领域 | 选型 | 说明 |
|------|------|------|
| 工程 | Vite + **TypeScript** | Node 工具链（用户确认） |
| UI 框架 | **Vue 3**（占位，前端阶段定稿） | 或 React，前端阶段确认 |
| 状态 | Pinia | 会话树 / 设置 / 流式状态 |
| PWA | `vite-plugin-pwa` | 离线缓存 + 安装到主屏 |
| 存储 | **IndexedDB** + Dexie.js | 结构化查询方便 |
| Markdown | markdown-it + highlight.js | 代码高亮 + 自定义代码块复制按钮 |
| 流式 | `fetch` + ReadableStream + AbortController | 流式读取 / 暂停 |
| 正则 | JS `RegExp` / `String.replace` | 后端正则处理 |
| 单价表 | 设置内可编辑的 JSON 价格表 | 见 §5.5 |

---

## 4. 数据模型（核心）

### 4.1 对话树节点 `MessageNode`

```ts
interface MessageNode {
  id: string;                 // UUID
  conversationId: string;
  role: 'user' | 'assistant';
  parentId: string | null;    // null = 根（root）的子节点
  content: string;            // 展示/上下文用内容（assistant 为正则处理后）
  rawContent?: string;        // assistant 原始输出（正则处理前），追溯/幂等用
  reasoning?: string;         // 思维链（assistant 专属，reasoner 模型）
  model?: string;             // 生成该消息的模型（assistant 专属，费用计算用）
  usage?: {                   // 本次 API 请求的 usage（assistant 专属）
    promptTokens: number;
    completionTokens: number;
    totalTokens: number;
    cost: number;             // 按该消息 model 单价折算，单位元
  };
  versionIndex: number;       // 同父兄弟中的实时序号，从 1 开始
  createdAt: number;
  updatedAt: number;
}
```

**树不变量**：
- 父子角色必须交替（user→assistant→user…）
- 根（无角色）的直接子节点**恒为 user**（会话首条必为用户消息）
- `versionIndex` = 同父兄弟实时序号（新增兄弟后重新编号）
- 版本指示器仅在**同父兄弟 ≥ 2** 时显示（需求 4）

### 4.2 对话 `Conversation`

```ts
interface Conversation {
  id: string;
  title: string;              // 由首条用户消息截断自动生成，可改
  model: string;              // 对话级模型，覆盖全局默认
  prompt: string;             // 对话级系统提示词
  regex: string;              // 对话级正则
  xRounds: number;            // 对话级附带轮数 X
  bgImage?: string;           // 对话级背景（覆盖全局）
  avatar?: string;            // 对话级头像（覆盖全局）
  activePath: string[];       // 当前显示路径（节点 id 序列，根→尾）
  createdAt: number;
  updatedAt: number;
}
```

### 4.3 全局设置 `GlobalSettings`

```ts
interface GlobalSettings {
  apiKey: string;
  defaultModel: string;               // 新建对话的默认模型
  models: string[];                   // 动态拉取的模型列表
  costTracking: boolean;              // 费用显示开关
  prompt: string;                     // 全局默认提示词
  regex: string;                      // 全局默认正则
  xRounds: number;                    // 全局默认 X
  bgImage: string;                    // 全局默认背景
  avatar: string;                     // 全局默认头像
  prices: Record<string, {            // 每模型单价（元 / 百万 token），可编辑
    in: number; out: number;
  }>;
}
```

---

## 5. 核心逻辑层（浏览器内承担"后端"职责）

### 5.1 分支/版本规则（已确认语义）

| 操作 | 行为 |
|------|------|
| **编辑 user** | 复制该 user 节点内容 → 在其父节点下新建同父 **user** 节点（版本+1），自动聚焦新节点等待 AI 回复 |
| **刷新 AI** | 在父 user 节点下新建同父 **assistant** 节点（版本+1），自动聚焦；流式进行中禁用 |
| **修改 AI 回复** | **就地覆盖**该节点 `content` / `reasoning`，不产生新节点；继续对话时上下文使用修改后内容 |
| **切换版本** | 路径截断到目标节点，**停在节点、手动再选**；尾部直接子节点以候选行渲染 |
| **尾部为待回复的 user** | 发送 = 为该 user 节点触发 AI 回复（输入框新文本丢弃并提示）；若其已有 AI 子节点，发送 = 新增 assistant 子节点（等同刷新） |
| **删除** | 整棵子树级联删除（全部版本 + 后继），二次确认 |
| **继续对话** | 尾部为 assistant 时输入 = 追加 user 子节点（角色交替）；尾部为 user 时按上一条处理 |

**候选行渲染规则**（"停在节点"的可操作落地）：
> 列表渲染 = 当前路径 + **尾部节点的直接子节点（作为候选行）**。候选行同样带版本指示器，点击候选行 = 沿其延伸路径。

**恢复旧分支示例**：
```
路径: 根→U1→R1b   编辑U1 → 根下新增 U2(A')，路径: 根→U2→R2(c)，U1 指示器变 1/2
点 U1 选 A →  路径停在 根→U1；a、b 渲染为候选（指示器 1/2、2/2）
点候选 b →  路径 根→U1→R1b，旧分支完整恢复；U2→R2 数据保留
```

### 5.2 API 集成层

- **端点**：`POST https://api.deepseek.com/chat/completions`；`GET /models`；`GET /user/balance`
- **鉴权**：`Authorization: Bearer <apiKey>`
- **请求体**：
  ```json
  {
    "model": "deepseek-chat",
    "messages": [ { "role": "system", "content": "..." }, ... ],
    "stream": true,
    "stream_options": { "include_usage": true }
  }
  ```
- **流式解析**：逐行读 `data: {...}`；`choices[0].delta.content`（正文）、`choices[0].delta.reasoning_content`（思维链，reasoner 专属）、末尾 chunk 携带 `usage`（需 include_usage，待实测）
- **暂停/中断**：`AbortController.abort()`；已收到的部分内容立即固化为 assistant 节点（`content`=部分，**无 usage**），可复制/修改/删除
- **错误处理**：网络错误、401（Key 无效）、429（限流）、5xx → 显示错误且不产生节点（或标记失败节点，前端阶段定）
- **模型切换**：对话级 `model` 字段 + 全局 `defaultModel`；切换后下一次请求即用新模型，历史消息费用仍按各自 `model` 单价计算

### 5.3 上下文组装（X 轮）

```
messages = []
if prompt:  messages.push({role:'system', content: prompt})   // 提示词默认必发
path = activePath（根→尾）
沿 path 从尾部向上收集最近 X 对（user+assistant）历史
  - assistant 消息 content 使用就地修改后的值，不含 reasoning（思维链不进上下文，待核对）
当前待回复的 user 消息作为最后一条 user 追加（不占 X）
```

- 不足 X 对则全量发；当前输入恒发送
- X 只沿**当前路径**计数，不跨分支
- **刷新 AI** 的上下文 = 该 assistant 节点的父 user 节点及以上路径历史（不含旧 assistant 自身），即"重新问一次"

### 5.4 正则处理管线

```
流式结束（rawContent 拼接完成）
  → content = applyRegex(rawContent)      // 处理一次后入库
手动修改回复
  → content = applyRegex(当前 content)     // 再处理一次
```

- 仅处理 **assistant 返回结果**（正文；思维链是否处理待定，默认不处理）
- **幂等约定**：正则需用户自行保证幂等（否则"加前缀"类正则会重复叠加）；文档内写明此约束
- 修改正则配置**不回溯**旧消息
- `rawContent` 保留原始输出，便于追溯

### 5.5 费用追踪

- **数据来源**：流式末 chunk 的 `usage`（`prompt_tokens` / `completion_tokens`）
- **费用计算**（实测确认含提示词缓存，命中价格低于未命中）：
  ```
  cost = prompt_cache_hit_tokens  × prices[model].cacheHit
       + prompt_cache_miss_tokens × prices[model].in
       + completion_tokens        × prices[model].out
  ```
  （单位：元 / 百万 token；若 usage 无缓存字段，则 hit=0、miss=`prompt_tokens` 兜底）
- **单价表**：`GlobalSettings.prices`，内置默认值并标注"以 DeepSeek 官网为准"，价格变动可在设置中改
- **余额**：`GET /user/balance` 单独接口（聊天接口不返回余额），返回 `balance_infos[]`（金额单位需实测）
- **显示**：每条 assistant 消息下方显示"输入/输出 token + 本次费用"；开关 `costTracking` 全局控制
- **备注**：暂停/中断的消息无 usage 记录；"本次输入 token"为当次请求完整 `prompt_tokens`（随历史累积）

### 5.6 消息增强逻辑

| 功能 | 逻辑 |
|------|------|
| 复制 | 纯文本 = 去 Markdown 标记的 content；Markdown = 原样 content；思维链 = reasoning（reasoner 专属，chat 模型禁用该项） |
| 修改回复 | 就地覆盖 content / reasoning；修改后过一遍正则（§5.4） |
| Markdown 渲染 | markdown-it + highlight.js；代码块一键复制按钮 |
| 思维链 | 可展开/折叠、可复制、可修改、可删除 |
| 删除 | 子树级联 + 二次确认 |
| 撤回 | **仅输入框**撤销/重做（compose 框与编辑框共用组件）；已发送消息不可撤回 |
| 输入框自适应 | 高度随内容增长（前端阶段） |

---

## 6. 功能清单（需求映射）

| # | 需求 | 归属 | 状态 |
|---|------|------|------|
| 1 | 费用追踪（token + 余额 + 开关） | 核心逻辑层 + API 层 | 设计中 |
| 2.1 | 复制回复（纯文本/Markdown/思维链） | 核心逻辑层 | 设计中 |
| 2.2 | 修改回复（含思维链，修改后作为上下文） | 核心逻辑层 | 设计中 |
| 2.3 | Markdown 渲染 + 代码高亮 + 一键复制 | UI 层（前端阶段） | 待办 |
| 2.4 | 刷新（生成分支） | 对话树引擎 | 设计中 |
| 2.5 | 删除（二次确认、子树级联） | 对话树引擎 | 设计中 |
| 2.6 | 撤回（输入框撤销/重做） | UI 层（前端阶段） | 待办 |
| 2.7 | 暂停（流式中断、部分内容可编辑） | API 集成层 + 树引擎 | 设计中 |
| 3 | 输入框自适应 | UI 层（前端阶段） | 待办 |
| 4 | 对话分支 + 版本指示器（多版本才显示） | 对话树引擎 + UI 层 | 设计中 |
| 5 | AI 增强（提示词 / 正则 / X 轮，全屏编辑） | 核心逻辑层 + UI 层 | 设计中 |
| 6 | 模型切换（动态拉取 + 默认模型 + 对话级切换） | API 层 + 设置 | 设计中 |
| 7 | 仿微信前端 + 首页 + 双 Tab + AI 头像设置 | UI 层（前端阶段） | 待办 |
| 8 | 背景/头像（全局默认 + 对话级覆盖） | 设置层 + UI 层 | 设计中 |

---

## 7. 持久化（IndexedDB）

| 表 | 键 | 内容 |
|----|----|------|
| `conversations` | id | Conversation 元数据（含 activePath） |
| `messages` | id（按 conversationId 索引） | MessageNode 全量 |
| `settings` | 单条 | GlobalSettings |

- 全部为浏览器本地存储，随 PWA 缓存，无需网络
- 大数据字段（背景/头像图片）以 base64/Blob 存 IndexedDB

---

## 8. 部署

- 构建产物：纯静态文件（`vite build`）
- **PWA 安装到手机主屏需要 HTTPS**（本地静态服务器走 LAN http 无法安装，但可作为普通网页正常使用）
- 推荐：免费静态托管（Netlify / Cloudflare Pages / GitHub Pages）→ 手机打开 → 添加到主屏；API Key 只存在手机本地，不经过托管服务器
- 备选：本地 `python -m http.server` / `vite preview` + 手机局域网访问（无安装、无离线，功能可用）

---

## 9. 待验证 / 风险项

1. ~~**CORS**~~ ✅ 已验证（2026-07-31，浏览器直连通过）
2. ~~**reasoning_content 多轮回传规则**~~ ✅ 已验证（API 接受携带回传；设计选择不回传）
3. ~~**`stream_options.include_usage` 是否返回 usage**~~ ✅ 已验证（流式末尾返回，含缓存字段）
4. **balance 接口返回单位**（元/分）—— 待实测
5. **DeepSeek 定价随时间变动** —— 单价表可编辑，内置值需标注时效
6. **提示词缓存实际计费比例** —— 缓存命中价格随官方调整，价格表需可编辑

---

## 10. 实施顺序（里程碑）

1. **可行性验证**：3 个验证脚本（CORS / include_usage / reasoning_content 回传）→ 一天内出结论
2. **数据层**：IndexedDB schema + 对话树引擎（纯函数，编辑/刷新/切换/删除/追加）+ 单元测试
3. **API 集成层**：流式解析、暂停（AbortController）、模型列表、余额
4. **逻辑管线**：上下文组装（X 轮）、正则管线、费用计算
5. **前端阶段**（另行规划）：仿微信 UI、消息渲染、版本指示器、设置页、PWA 打包
6. **部署**：静态托管 + 手机验证

---

## 附：已确认的设计决策（本次评审结论）

- AI 回复修改 = **就地覆盖**（刷新才新建版本）
- 分支切换 = **停在节点、手动再选**（尾部直接子节点渲染为候选行）
- 尾部为待回复 user 时发送 = **触发该 user 的 AI 回复**（丢弃新文本并提示）
- 删除 = **整棵子树级联**
- 思维链 **不进上下文**（仅展示/复制/修改；实测确认 API 接受携带回传，但为省 token 选择不回传）
- 费用公式区分 **prompt 缓存命中/未命中** 价格（实测 usage 含缓存字段）
- X 轮 = 最近 X 对历史 + 当前输入 + 系统提示词（当前输入不占 X）
- 纯前端 PWA + TypeScript 工程（Vite），无后端服务
