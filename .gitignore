# 依赖与构建产物（CI 中 npm ci + build 生成）
node_modules
dist

# 本地下载的 APK 产物（云端构建，不入库）
app-debug.apk

# 本地环境
*.local
.env
.env.*
!.env.example

# 编辑器 / 系统
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/extensions.json
*.log
