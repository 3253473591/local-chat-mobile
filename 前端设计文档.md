# 前端设计文档

> 基于 [设计文档.md](设计文档.md) 的纯前端 PWA 方案
> UI 框架：Vue 3 (Composition API) + Tailwind CSS 4 + Pinia

---

## 1. 页面结构与路由

Hash 路由，仿微信页面转场：

| 路径 | 视图 | 说明 |
|------|------|------|
| `#/` | HomeView | 首页（对话列表 + 双 Tab） |
| `#/chat/:id` | ChatView | 对话页（消息气泡 + 输入） |
| `#/chat/:id/enhance` | AIEnhanceView | AI 增强整页（模型/提示词/正则/思考/外观） |
| `#/settings` | SettingsView | 设置主列表（导航入口） |
| `#/settings/api` | ApiSettings | API 设置（Key + 余额） |
| `#/settings/model` | ModelSettings | 模型（默认模型选择） |
| `#/settings/ai` | AiDefaults | AI 增强默认 |
| `#/settings/cost` | CostSettings | 费用（追踪/峰谷/单价） |
| `#/settings/appearance` | AppearanceSettings | 外观（背景/双头像） |

---

## 2. 通用组件

### 2.1 HeaderBar
微信风格白底页头：左返回箭头 `‹`（RouterLink）+ 居中标题（17px medium）+ 右侧插槽；底边 0.5px 细线；safe-area-inset-top。

### 2.2 SettingGroup / SettingRow
仿微信"我"/朋友资料页：
- **SettingGroup**：白卡片 + 上 0.5px 下 0.5px 粗线 + 8px 灰色 `bg-bg` 间隔
- **SettingRow**：高 52px（h-13），左 label（15px 黑），右 value（15px 灰）+ 箭头 `›`（#c7c7cc）
- **细分隔线**：左缩进 16px（对齐 label 文字），CSS `::before` 伪元素实现
- clickable 行 hover active:bg-bg

### 2.3 ActionSheet
底部弹出菜单（遮罩 + 白色圆角面板 + 取消按钮）；safe-area-inset-bottom。

### 2.4 FullscreenEditor
全屏覆盖编辑器：标题 + 确认/关闭+ 多行 textarea + 思维链编辑（assistant 模式）+ 正则实时测试区（输入→输出）。

### 2.5 MessageBubble
- **User 气泡**：右侧绿色 `bg-bubble-user (#95EC69)`，rounded-2xl，右上直角
- **AI 气泡**：左侧白色 `bg-bubble-ai`，rounded-2xl，左上直角
- **候选行**：虚线边框 + 半透明白
- **思维链**：折叠按钮 `🧠 思维链 ▼/▲`，灰色背景区
- **Markdown**：`MarkdownRenderer` 渲染
- **费用**：↑/↓ token + ¥金额（实时按当前价格+峰谷重算）
- **流式光标**：蓝色闪烁 `▍`
- **头像**：AI 左侧圆圈、用户右侧圆圈（对话级覆盖 → 全局）

### 2.6 VersionIndicator
版本下拉选择器：仅 `siblings.length >= 2` 时显示；点击展开下拉面板，显示各版本预览 + 选中高亮。

### 2.7 Composer
自适应 textarea（最大 200px）+ 发送/暂停按钮；发送按钮 `bg-accent rounded-full`（胶囊形）；暂停按钮 `bg-danger rounded-full`。

### 2.8 MarkdownRenderer
markdown-it + highlight.js；代码块右上角"复制"按钮；自实现 escapeHtml 消除 markdown-it 自引用类型问题。

---

## 3. 视图详情

### 3.1 HomeView（首页）
- HeaderBar："对话"标题 + 右上角 "＋" 新建
- 对话列表：AI 头像（对话级→全局） + 标题（📌 置顶标记） + 模型名 + 时间
- **长按/右键** → ActionSheet：重命名 / 置顶(取消置顶) / 删除
- 底部 TabBar：💬对话 / ⚙️设置（RouterLink + active-class）
- 置顶排序：pinnedAt 优先 → updatedAt 倒序

### 3.2 ChatView（对话页）
- HeaderBar：对话标题 + 右侧"清空"按钮 + AI 头像（点进入 AIEnhanceView）
- 消息列表：pathNodes（正常气泡） + candidates（候选行）
- 错误条：红色半透明背景，显示 `convStore.error`
- 背景：对话级→全局，CSS backgroundImage + cover
- Composer 固定在底部
- 自动滚动底：watch activePath + streamingNode 变化

### 3.3 SettingsView（设置主列表）
6 个 SettingGroup 入口，每行 `value + ›`：
- API 设置（Key 预览）
- 模型（当前默认模型）
- AI 增强默认
- 费用与计费（已开启/关闭追踪）
- 外观

点击进入二级详情页。

### 3.4 AIEnhanceView（AI 增强整页）
仿微信朋友资料页，列表式条目：
- **模型**：列表选择 + ✓ 标记
- **提示词/正则**：点击打开 FullscreenEditor
- **替换为**：正则 replacement 输入
- **X 轮**：number input
- **思考模式**：跟随全局 / 开启 / 关闭（三态下拉）
- **思考深度**：跟随全局 / low / high / max
- **温度**：跟随全局 / 输入值
- **外观**（本对话）：背景/AI头像/用户头像上传
- 新建对话时带 `?new=1` → 自动跳转此页

### 3.5 二级设置页
全部用 HeaderBar + SettingGroup 结构：
- **ApiSettings**：Key 输入 + 查询余额
- **ModelSettings**：刷新模型列表 + 模型选择（✓ 标记默认）
- **AiDefaults**：提示词/正则（全屏编辑）+ 替换为 + X轮 + 思考开关 + 深度 + 温度
- **CostSettings**：追踪开关 + 峰谷规则（时段增删）+ 单价表（模型增删 + 三栏价格）
- **AppearanceSettings**：背景/AI头像/用户头像上传预览清除

---

## 4. Pinia Stores

### 4.1 conversationStore
核心状态：conversations / currentId / nodes / activePath / candidates / isStreaming / error
- 树操作封装：sendUserMessage / editUserMessage / deleteSubtree / refreshAIReply / switchVersion
- 流式管线：replyToUserNode → runStream（增量+正则+usage）
- AI 增强：setModel / setEnhance / setAppearance
- 对话管理：create / enter / clear / rename / togglePin / deleteConversation

### 4.2 settingsStore
- 设置读写 + 模型拉取 + 余额查询 + 价格迁移 + 模型去重
- 自动保存（500ms 防抖 watch）

### 4.3 uiStore
- Toast / ActionSheet / FullscreenEditor 状态管理

---

## 5. 主题色板

Tailwind v4 `@theme` 定义（`src/style.css`）：

| Token | 值 | 用途 |
|-------|-----|------|
| `bg` | #EDEDED | 页面背景 |
| `bubble-user` | #95EC69 | 用户气泡 |
| `bubble-ai` | #FFFFFF | AI 气泡 |
| `reasoning` | #F5F5F5 | 思维链区 |
| `ink` | #000000 | 正文 |
| `sub` | #999999 | 次要文字 |
| `line` | #E5E5E5 | 分隔线 |
| `accent` | #576B95 | 链接/可点击 |
| `danger` | #FA5151 | 删除 |

---

## 6. 交互细节

| 功能 | 触发 | 效果 |
|------|------|------|
| 操作菜单 | 长按消息/对话项（500ms）| ActionSheet 弹出；PC 右键也支持 |
| 编辑消息 | 长按 → 编辑/修改 | FullscreenEditor（user→新版本；AI→就地覆盖） |
| 删除消息 | 长按 → 删除 | user→轮次删除+接续；AI→子树级联；二次确认 |
| 删除对话 | 长按对话项 → 删除 | 联表删除（IndexedDB 事务） |
| 刷新 AI | 长按 AI 消息 → 刷新 | 同父新建版本 + 重新流式 |
| 复制 | 长按 AI → 复制·纯文本/Markdown/思维链 | 局域网 HTTP 降级 execCommand |
| 版本切换 | 点击版本指示器 → 选版本 | auto-extend 到叶子 |
| 候选行 | 点击虚线候选行 | extendPath 延伸路径 |
| 暂停 | 流式中点击红色"暂停" | abort + 保留部分内容 |
| 新建对话 | 首页"＋" → 自动跳转 AIEnhanceView | `?new=1` 标记 |
| 清空聊天 | 对话页 header "清空" | 二级确认 + 删除全部消息 |

---

## 7. PWA 配置

- `vite-plugin-pwa`：autoUpdate + generateSW
- manifest：name"本地对话"，display standalone，theme/bg color #EDEDED
- icons：192/512 PNG（深蓝圆角 + 白色对话气泡，纯 Node 手写 PNG 编码生成）
- runtimeCaching：DeepSeek API → NetworkOnly（不缓存）
- 注意：局域网 HTTP 无法触发 PWA 安装（需 HTTPS）

---

## 8. 性能

- Markdown 流式中只追加纯文本，结束后完整渲染一次（避免频繁 DOM 重建）
- IndexedDB 批量操作（bulkPut/bulkDelete + transaction）
- 图片上传 canvas 压缩

---

## 9. 组件清单

| 组件 | 路径 | 说明 |
|------|------|------|
| `App.vue` | src/App.vue | 根容器（max-w-[430px] 手机居中）+ Toast + 全局弹层 |
| `HomeView.vue` | src/views/ | 首页对话列表 |
| `ChatView.vue` | src/views/ | 对话页 |
| `SettingsView.vue` | src/views/ | 设置主列表 |
| `AIEnhanceView.vue` | src/views/ | AI 增强整页 |
| `ApiSettings.vue` | src/views/settings/ | API 设置 |
| `ModelSettings.vue` | src/views/settings/ | 模型设置 |
| `AiDefaults.vue` | src/views/settings/ | AI 增强默认 |
| `CostSettings.vue` | src/views/settings/ | 费用设置 |
| `AppearanceSettings.vue` | src/views/settings/ | 外观设置 |
| `HeaderBar.vue` | src/components/ | 微信风格页头 |
| `SettingGroup.vue` | src/components/ | 分组容器 |
| `SettingRow.vue` | src/components/ | 设置行 |
| `MessageBubble.vue` | src/components/ | 消息气泡 |
| `MarkdownRenderer.vue` | src/components/ | Markdown 渲染 |
| `VersionIndicator.vue` | src/components/ | 版本指示器 |
| `Composer.vue` | src/components/ | 输入区 |
| `ActionSheet.vue` | src/components/ | 底部菜单 |
| `FullscreenEditor.vue` | src/components/ | 全屏编辑器 |
